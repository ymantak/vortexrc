{
  "name": "cras-auto-services-template",
  "version": "1.0.0",
  "private": true,
  "description": "SCSS build scripts for the CRAS HTML template",
  "scripts": {
    "css:build": "sass assets/sass/style.scss assets/sass/style.css",
    "css:watch": "sass --watch assets/sass/style.scss:assets/sass/style.css"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
